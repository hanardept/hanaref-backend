{
  "name": "hanaref-api-tests",
  "version": "1.0.0",
  "description": "API tests for hanaref-backend",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-html-reporter": "^3.8.0"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-html-reporter", {
        "outputPath": "./results/test-report.html"
      }]
  ]
}
}